package org.esfe.easymenu.modelos;

// Los 3 roles que puede tener un usuario del personal (Cliente NO inicia sesión,
// por eso no aparece aquí — el sistema es autoservicio para el cliente).
public enum RolUsuario {
    COCINA,
    CAJERO,
    ADMINISTRADOR
}
